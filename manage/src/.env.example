# Copy this file to `.env` and adjust as needed:  cp .env.example .env
#
# The values below match the vLLM server running on the Ubuntu box
# (Qwen/Qwen3-8B-AWQ on port 8000). Confirm with:
#   curl http://localhost:8000/v1/models

# Local vLLM OpenAI-compatible endpoint. Keep the trailing /v1.
VLLM_API_BASE=http://localhost:8000/v1

# Model id EXACTLY as served by vLLM (the `id` field from /v1/models).
# Qwen3-8B-AWQ is the pick for the 11 GB RTX 2080 Ti (Turing): ~5.5 GB of
# 4-bit weights, roomy KV cache, no exotic kernels needed.
VLLM_MODEL=Qwen/Qwen3-8B-AWQ

# vLLM ignores the API key unless it was started with --api-key.
# LiteLLM still requires a non-empty value, so leave this as-is.
VLLM_API_KEY=EMPTY

# Qwen3 is a hybrid reasoning model. Keep thinking OFF for fast, clean,
# JSON-only summaries; set to `true` to let the model emit <think> traces.
VLLM_ENABLE_THINKING=false

# Optional: override where summarize.py looks for dumps (default: repo-root/dump).
# DUMP_DIR=/absolute/path/to/dump
